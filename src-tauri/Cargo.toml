[package]
name = "shairee"
version = "1.0.2"
description = "Ultra-fast local file sharing from Windows to Android"
authors = ["Shairee"]
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "shairee_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
# Tauri core
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-dialog = "2"
tauri-plugin-shell = "2"

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Async runtime
tokio = { version = "1", features = ["full"] }

# HTTP server
actix-web = "4"
actix-files = "0.6"
actix-cors = "0.7"
actix-multipart = "0.7"
actix-ws = "0.3"
futures-util = "0.3"
tokio-util = { version = "0.7", features = ["codec"] }

# QR code
qrcode = "0.14"
image = { version = "0.25", default-features = false, features = ["png"] }
base64 = "0.22"

# Network
local-ip-address = "0.6"
reqwest = { version = "0.12", default-features = false, features = ["stream"] }

# Utilities
uuid = { version = "1", features = ["v4"] }
mime_guess = "2"
percent-encoding = "2"
log = "0.4"
env_logger = "0.11"
parking_lot = "0.12"
humansize = "2"
rand = "0.8"
chrono = { version = "0.4", features = ["serde"] }
thiserror = "2"
walkdir = "2"
zip = { version = "2", default-features = false, features = ["deflate"] }
open = "5"

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
